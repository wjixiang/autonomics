[project]
name = "thousand-genomes"
version = "0.1.0"
description = "boto3 client and helpers for the 1000 Genomes public S3 bucket (s3://1000genomes)"
readme = "README.md"
authors = [{ name = "wjixiang", email = "wjixiang27@gmail.com" }]
requires-python = ">=3.13"
dependencies = ["boto3"]

[build-system]
requires = ["uv_build>=0.11.29,<0.12.0"]
build-backend = "uv_build"
