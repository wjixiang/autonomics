[project]
name = "datalake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "wjixiang", email = "wjixiang27@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "polars>=1.39.3",
    "pyarrow>=23.0.1",
    "pyiceberg>=0.11.1",
]

[project.scripts]
datalake = "datalake.cli:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
